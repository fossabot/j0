!function() {
    "use strict";
    function t(t, e) {
        if (!t) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
        return !e || "object" != typeof e && "function" != typeof e ? t : e;
    }
    function e(t, e) {
        if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function, not " + typeof e);
        t.prototype = Object.create(e && e.prototype, {
            constructor: {
                value: t,
                enumerable: !1,
                writable: !0,
                configurable: !0
            }
        }), e && (Object.setPrototypeOf ? Object.setPrototypeOf(t, e) : t.__proto__ = e);
    }
    function n(t, e) {
        if (!(t instanceof e)) throw new TypeError("Cannot call a class as a function");
    }
    function r(t) {
        return V.isArray(t) ? t : V.from(t);
    }
    function o(t, e) {
        return e = {
            exports: {}
        }, t(e, e.exports), e.exports;
    }
    function i(t, e) {
        var n = new Gt(), r = new Promise(function(r, o) {
            switch (n.onload = function() {
                r(n.result);
            }, n.onerror = function() {
                o(n.error);
            }, e) {
              case "ArrayBuffer":
                n.readAsArrayBuffer(t);
                break;

              case "BinaryString":
                n.readAsBinaryString(t);
                break;

              case "DataURL":
                n.readAsDataURL(t);
                break;

              default:
                n.readAsText(t);
            }
        });
        return r.reader = n, r;
    }
    function a(t) {
        return t.trim();
    }
    function u(t) {
        return "function" == typeof t;
    }
    function s(t, e, n) {
        var r = arguments.length > 3 && void 0 !== arguments[3] ? arguments[3] : 0, o = t.length, i = t[M] ? t[M]() : t;
        if (0 <= o) {
            for (var a = r; a < o; a += 1) if (e.call(n, t[a], a, t)) return;
        } else if (u(i.next)) for (var s = 0; s < z; ) {
            var c = i.next(), f = c.value, l = c.done;
            if (l || r <= s && e.call(n, f, s, t)) return;
            s += 1;
        } else {
            var h = r, d = !0, y = !1, v = void 0;
            try {
                for (var p, w = t[Symbol.iterator](); !(d = (p = w.next()).done); d = !0) {
                    var m = p.value;
                    if (e.call(n, m, h, t)) return;
                    h += 1;
                }
            } catch (t) {
                y = !0, v = t;
            } finally {
                try {
                    !d && w.return && w.return();
                } finally {
                    if (y) throw v;
                }
            }
        }
    }
    function c(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : new ut();
        return s(a(t).split("&"), function(t) {
            if (t) {
                var n = t.split("="), o = r(n), i = o[0], a = o.slice(1);
                i = dt(i.replace(/\+/g, " ")), a = dt(a.join("=").replace(/\+/g, " ")), e.append(i, a);
            }
        }), e;
    }
    function f(t) {
        return "string" == typeof t;
    }
    function l() {
        var t = this, e = this.length, n = 0;
        return {
            next: function() {
                return {
                    value: t[n],
                    done: e <= n++
                };
            }
        };
    }
    function h(t) {
        for (var e = arguments.length, n = V(e > 1 ? e - 1 : 0), r = 1; r < e; r++) n[r - 1] = arguments[r];
        return Xt.apply(t, n);
    }
    function d(t) {
        return t;
    }
    function y(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : d, n = arguments[2], r = [];
        return s(t, function(o, i) {
            h(r, e.call(n, o, i, t));
        }), r;
    }
    function v(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : ",";
        return y(t).join(e);
    }
    function p(t) {
        for (var e = et(t, 10), n = [], r = 0; r < e; r += 1) h(n, this);
        return v(n, "");
    }
    // let firstImmediate = true;
    // let immediateCount = 0;
    // const tasks = {};
    // const suffix = `_setImmediate${window.immediateId}`;
    // function setImmediatePostMessage(fn) {
    // 	if (firstImmediate) {
    // 		firstImmediate = false;
    // 		addEventListner(window, 'message', function ({data}) {
    // 			if (data.split) {
    // 				const [key] = data.split(suffix);
    // 				const task = tasks[key];
    // 				if (task) {
    // 					task();
    // 				}
    // 				delete tasks[key];
    // 			}
    // 		});
    // 	}
    // 	immediateCount += 1;
    // 	postMessage(`${immediateCount}${suffix}`, '*');
    // 	tasks[immediateCount] = fn;
    // 	return immediateCount;
    // }
    function w(t) {
        return ct(t);
    }
    function m(t, e) {
        var n = 1;
        t(function() {
            4 == (n *= 2) && e();
        }), n += 1;
    }
    function g(t) {
        return t && u(t.then) && u(t.catch);
    }
    function b(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : d, n = arguments[2], r = void 0;
        return s(t, function(o, i) {
            if (e.call(n, o, i, t)) return r = o, !0;
        }), r;
    }
    function k(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : d, n = arguments[2], r = -1;
        return s(t, function(o, i) {
            if (e.call(n, o, i, t)) return r = i, !0;
        }), r;
    }
    function x(t) {
        for (var e = arguments.length, n = V(e > 1 ? e - 1 : 0), r = 1; r < e; r++) n[r - 1] = arguments[r];
        return t.splice.apply(t, n);
    }
    function O() {
        return this.entries();
    }
    function _() {
        var t = this, e = this.length, n = 0;
        return {
            next: function() {
                return {
                    value: t[n],
                    done: e <= n++
                };
            }
        };
    }
    function j() {
        var t = this, e = this.length, n = 0;
        return {
            next: function() {
                return {
                    value: t[n],
                    done: e <= n++
                };
            }
        };
    }
    function A() {
        var t = this, e = this.length, n = 0;
        return {
            next: function() {
                return {
                    value: t[n],
                    done: e <= n++
                };
            }
        };
    }
    function E(t) {
        var e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : d, n = arguments[2], r = [];
        return s(t, function(t, o, i) {
            e.call(n, t, o, i) && h(r, t);
        }), r;
    }
    function L(t, e, n) {
        for (var r in t) if (t.hasOwnProperty(r) && e.call(n, t[r], r, t)) return;
    }
    function S(t) {
        return ("" + t).toLowerCase();
    }
    function T(t) {
        return void 0 === t;
    }
    function P(t, e) {
        return t instanceof e;
    }
    function B(t) {
        return 0 <= k(he, function(e) {
            return P(t, e);
        });
    }
    /* eslint-disable no-bitwise */
    function F(t, e, n) {
        for (var r = ve[n], o = 0, i = 0; 0 < n--; ) {
            var a = 0 === n ? r : ye, u = pe * i++;
            o |= (t[e + n] & a) << u;
        }
        return Y.fromCharCode(o);
    }
    /* eslint-enable no-bitwise */
    function R(t) {
        for (var e = new wt(t), n = [], r = 0; r < e.length; r++) {
            var o = e[r], i = void 0;
            i = o < 128 ? 1 : o < 224 ? 2 : o < 240 ? 3 : o < 248 ? 4 : o < 252 ? 5 : 6, h(n, F(e, r, i)), 
            r += i - 1;
        }
        return n.join("");
    }
    function U(t) {
        if (t.slice) return t.slice(0);
        var e = new wt(t.byteLength);
        return e.set(new wt(t)), e.buffer;
    }
    function I(t) {
        var e = new Headers();
        return t.replace(/\r?\n[\t ]+/, " ").split(/\r?\n/).forEach(function(t) {
            var n = t.split(":"), o = r(n), i = o[0], u = o.slice(1);
            i && e.append(a(i), a(u.join(":")));
        }), e;
    }
    function N(t, e) {
        return new Nt(function(n, r) {
            var o = new me(t, e), i = new Ut();
            i.onload = function() {
                var t = {
                    status: i.status,
                    statusText: i.statusText,
                    headers: I(i.getAllResponseHeaders() || "")
                };
                t.url = "responseURL" in i ? i.responseURL : t.headers.get("X-Request-URL");
                var e = "response" in i ? i.response : i.responseText;
                n(new xe(e, t));
            }, i.onerror = function() {
                r(new TypeError("Network request failed"));
            }, i.ontimeout = function() {
                r(new TypeError("Network request failed"));
            }, i.open(o.method, o.url, !0), "include" === o.credentials && (i.withCredentials = !0), 
            i.responseType = "blob", s(o.headers, function(t) {
                var e = q(t, 2), n = e[0], r = e[1];
                i.setRequestHeader(n, r);
            }), i.send(T(o.bodyInit) ? null : o.bodyInit);
        });
    }
    var D = function t(e, n, r) {
        null === e && (e = Function.prototype);
        var o = Object.getOwnPropertyDescriptor(e, n);
        if (void 0 === o) {
            var i = Object.getPrototypeOf(e);
            return null === i ? void 0 : t(i, n, r);
        }
        if ("value" in o) return o.value;
        var a = o.get;
        if (void 0 !== a) return a.call(r);
    }, q = function() {
        function t(t, e) {
            var n = [], r = !0, o = !1, i = void 0;
            try {
                for (var a, u = t[Symbol.iterator](); !(r = (a = u.next()).done) && (n.push(a.value), 
                !e || n.length !== e); r = !0) ;
            } catch (t) {
                o = !0, i = t;
            } finally {
                try {
                    !r && u.return && u.return();
                } finally {
                    if (o) throw i;
                }
            }
            return n;
        }
        return function(e, n) {
            if (V.isArray(e)) return e;
            if (Symbol.iterator in Object(e)) return t(e, n);
            throw new TypeError("Invalid attempt to destructure non-iterable instance");
        };
    }(), C = function() {
        function t(t, e) {
            for (var n = 0; n < e.length; n++) {
                var r = e[n];
                r.enumerable = r.enumerable || !1, r.configurable = !0, "value" in r && (r.writable = !0), 
                Object.defineProperty(t, r.key, r);
            }
        }
        return function(e, n, r) {
            return n && t(e.prototype, n), r && t(e, r), e;
        };
    }(), G = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(t) {
        return typeof t;
    } : function(t) {
        return t && "function" == typeof Symbol && t.constructor === Symbol && t !== Symbol.prototype ? "symbol" : typeof t;
    }, H = "undefined" != typeof window ? window : "undefined" != typeof global ? global : "undefined" != typeof self ? self : {}, M = (o(function(t) {
        /**
  * Copyright (c) 2014, Facebook, Inc.
  * All rights reserved.
  *
  * This source code is licensed under the BSD-style license found in the
  * https://raw.github.com/facebook/regenerator/master/LICENSE file. An
  * additional grant of patent rights can be found in the PATENTS file in
  * the same directory.
  */
        !function(e) {
            function n(t, e, n, r) {
                // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
                var i = e && e.prototype instanceof o ? e : o, a = Object.create(i.prototype), u = new d(r || []);
                // The ._invoke method unifies the implementations of the .next,
                // .throw, and .return methods.
                return a._invoke = c(t, n, u), a;
            }
            // Try/catch helper to minimize deoptimizations. Returns a completion
            // record like context.tryEntries[i].completion. This interface could
            // have been (and was previously) designed to take a closure to be
            // invoked without arguments, but in all the cases we care about we
            // already have an existing method we want to call, so there's no need
            // to create a new function object. We can even get away with assuming
            // the method takes exactly one argument, since that happens to be true
            // in every case, so we don't have to touch the arguments object. The
            // only additional allocation required is the completion record, which
            // has a stable shape and so hopefully should be cheap to allocate.
            function r(t, e, n) {
                try {
                    return {
                        type: "normal",
                        arg: t.call(e, n)
                    };
                } catch (t) {
                    return {
                        type: "throw",
                        arg: t
                    };
                }
            }
            // Dummy constructor functions that we use as the .constructor and
            // .constructor.prototype properties for functions that return Generator
            // objects. For full spec compliance, you may wish to configure your
            // minifier not to mangle the names of these two functions.
            function o() {}
            function i() {}
            function a() {}
            // Helper for defining the .next, .throw, and .return methods of the
            // Iterator interface in terms of a single ._invoke method.
            function u(t) {
                [ "next", "throw", "return" ].forEach(function(e) {
                    t[e] = function(t) {
                        return this._invoke(e, t);
                    };
                });
            }
            function s(t) {
                function n(e, o, i, a) {
                    var u = r(t[e], t, o);
                    if ("throw" !== u.type) {
                        var s = u.arg, c = s.value;
                        return c && "object" === (void 0 === c ? "undefined" : G(c)) && m.call(c, "__await") ? Promise.resolve(c.__await).then(function(t) {
                            n("next", t, i, a);
                        }, function(t) {
                            n("throw", t, i, a);
                        }) : Promise.resolve(c).then(function(t) {
                            // When a yielded Promise is resolved, its final value becomes
                            // the .value of the Promise<{value,done}> result for the
                            // current iteration. If the Promise is rejected, however, the
                            // result for this iteration will be rejected with the same
                            // reason. Note that rejections of yielded Promises are not
                            // thrown back into the generator function, as is the case
                            // when an awaited Promise is rejected. This difference in
                            // behavior between yield and await is important, because it
                            // allows the consumer to decide what to do with the yielded
                            // rejection (swallow it and continue, manually .throw it back
                            // into the generator, abandon iteration, whatever). With
                            // await, by contrast, there is no opportunity to examine the
                            // rejection reason outside the generator function, so the
                            // only option is to throw it from the await expression, and
                            // let the generator function handle the exception.
                            s.value = t, i(s);
                        }, a);
                    }
                    a(u.arg);
                }
                function o(t, e) {
                    function r() {
                        return new Promise(function(r, o) {
                            n(t, e, r, o);
                        });
                    }
                    // If enqueue has been called before, then we want to wait until
                    // all previous Promises have been resolved before calling invoke,
                    // so that results are always delivered in the correct order. If
                    // enqueue has not been called before, then it is important to
                    // call invoke immediately, without waiting on a callback to fire,
                    // so that the async generator function has the opportunity to do
                    // any necessary setup in a predictable way. This predictability
                    // is why the Promise constructor synchronously invokes its
                    // executor callback, and why async functions synchronously
                    // execute code before the first await. Since we implement simple
                    // async functions in terms of async generators, it is especially
                    // important to get this right, even though it requires care.
                    // Avoid propagating failures to Promises returned by later
                    // invocations of the iterator.
                    return i = i ? i.then(r, r) : r();
                }
                "object" === G(e.process) && e.process.domain && (n = e.process.domain.bind(n));
                var i;
                // Define the unified helper method that is used to implement .next,
                // .throw, and .return (see defineIteratorMethods).
                this._invoke = o;
            }
            function c(t, e, n) {
                var o = j;
                return function(i, a) {
                    if (o === E) throw new Error("Generator is already running");
                    if (o === L) {
                        if ("throw" === i) throw a;
                        // Be forgiving, per 25.3.3.3.3 of the spec:
                        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
                        return v();
                    }
                    for (n.method = i, n.arg = a; ;) {
                        var u = n.delegate;
                        if (u) {
                            var s = f(u, n);
                            if (s) {
                                if (s === S) continue;
                                return s;
                            }
                        }
                        if ("next" === n.method) // Setting context._sent for legacy support of Babel's
                        // function.sent implementation.
                        n.sent = n._sent = n.arg; else if ("throw" === n.method) {
                            if (o === j) throw o = L, n.arg;
                            n.dispatchException(n.arg);
                        } else "return" === n.method && n.abrupt("return", n.arg);
                        o = E;
                        var c = r(t, e, n);
                        if ("normal" === c.type) {
                            if (// If an exception is thrown from innerFn, we leave state ===
                            // GenStateExecuting and loop back for another invocation.
                            o = n.done ? L : A, c.arg === S) continue;
                            return {
                                value: c.arg,
                                done: n.done
                            };
                        }
                        "throw" === c.type && (o = L, // Dispatch the exception by looping back around to the
                        // context.dispatchException(context.arg) call above.
                        n.method = "throw", n.arg = c.arg);
                    }
                };
            }
            // Call delegate.iterator[context.method](context.arg) and handle the
            // result, either by returning a { value, done } result from the
            // delegate iterator, or by modifying context.method and context.arg,
            // setting context.delegate to null, and returning the ContinueSentinel.
            function f(t, e) {
                var n = t.iterator[e.method];
                if (n === p) {
                    if (// A .throw or .return when the delegate iterator has no .throw
                    // method always terminates the yield* loop.
                    e.delegate = null, "throw" === e.method) {
                        if (t.iterator.return && (// If the delegate iterator has a return method, give it a
                        // chance to clean up.
                        e.method = "return", e.arg = p, f(t, e), "throw" === e.method)) // If maybeInvokeDelegate(context) changed context.method from
                        // "return" to "throw", let that override the TypeError below.
                        return S;
                        e.method = "throw", e.arg = new TypeError("The iterator does not provide a 'throw' method");
                    }
                    return S;
                }
                var o = r(n, t.iterator, e.arg);
                if ("throw" === o.type) return e.method = "throw", e.arg = o.arg, e.delegate = null, 
                S;
                var i = o.arg;
                // Assign the result of the finished delegate to the temporary
                // variable specified by delegate.resultName (see delegateYield).
                // Resume execution at the desired location (see delegateYield).
                // If context.method was "throw" but the delegate handled the
                // exception, let the outer generator proceed normally. If
                // context.method was "next", forget context.arg since it has been
                // "consumed" by the delegate iterator. If context.method was
                // "return", allow the original .return call to continue in the
                // outer generator.
                // The delegate iterator is finished, so forget it and continue with
                // the outer generator.
                return i ? i.done ? (e[t.resultName] = i.value, e.next = t.nextLoc, "return" !== e.method && (e.method = "next", 
                e.arg = p), e.delegate = null, S) : i : (e.method = "throw", e.arg = new TypeError("iterator result is not an object"), 
                e.delegate = null, S);
            }
            function l(t) {
                var e = {
                    tryLoc: t[0]
                };
                1 in t && (e.catchLoc = t[1]), 2 in t && (e.finallyLoc = t[2], e.afterLoc = t[3]), 
                this.tryEntries.push(e);
            }
            function h(t) {
                var e = t.completion || {};
                e.type = "normal", delete e.arg, t.completion = e;
            }
            function d(t) {
                // The root entry object (effectively a try statement without a catch
                // or a finally block) gives us a place to store values thrown from
                // locations where there is no enclosing try statement.
                this.tryEntries = [ {
                    tryLoc: "root"
                } ], t.forEach(l, this), this.reset(!0);
            }
            function y(t) {
                if (t) {
                    var e = t[b];
                    if (e) return e.call(t);
                    if ("function" == typeof t.next) return t;
                    if (!isNaN(t.length)) {
                        var n = -1, r = function e() {
                            for (;++n < t.length; ) if (m.call(t, n)) return e.value = t[n], e.done = !1, e;
                            return e.value = p, e.done = !0, e;
                        };
                        return r.next = r;
                    }
                }
                // Return an iterator with no values.
                return {
                    next: v
                };
            }
            function v() {
                return {
                    value: p,
                    done: !0
                };
            }
            var p, w = Object.prototype, m = w.hasOwnProperty, g = "function" == typeof Symbol ? Symbol : {}, b = g.iterator || "@@iterator", k = g.asyncIterator || "@@asyncIterator", x = g.toStringTag || "@@toStringTag", O = !0, _ = e.regeneratorRuntime;
            if (_) // Don't bother evaluating the rest of this file if the runtime was
            // already defined globally.
            // If regeneratorRuntime is defined globally and we're in a module,
            // make the exports object identical to regeneratorRuntime.
            return void (O && (t.exports = _));
            // Define the runtime globally (as expected by generated code) as either
            // module.exports (if we're in a module) or a new, empty object.
            _ = e.regeneratorRuntime = O ? t.exports : {}, _.wrap = n;
            var j = "suspendedStart", A = "suspendedYield", E = "executing", L = "completed", S = {}, T = {};
            T[b] = function() {
                return this;
            };
            var P = Object.getPrototypeOf, B = P && P(P(y([])));
            B && B !== w && m.call(B, b) && (// This environment has a native %IteratorPrototype%; use it instead
            // of the polyfill.
            T = B);
            var F = a.prototype = o.prototype = Object.create(T);
            i.prototype = F.constructor = a, a.constructor = i, a[x] = i.displayName = "GeneratorFunction", 
            _.isGeneratorFunction = function(t) {
                var e = "function" == typeof t && t.constructor;
                // For the native GeneratorFunction constructor, the best we can
                // do is to check its .name property.
                return !!e && (e === i || "GeneratorFunction" === (e.displayName || e.name));
            }, _.mark = function(t) {
                return Object.setPrototypeOf ? Object.setPrototypeOf(t, a) : (t.__proto__ = a, x in t || (t[x] = "GeneratorFunction")), 
                t.prototype = Object.create(F), t;
            }, // Within the body of any async function, `await x` is transformed to
            // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
            // `hasOwn.call(value, "__await")` to determine if the yielded value is
            // meant to be awaited.
            _.awrap = function(t) {
                return {
                    __await: t
                };
            }, u(s.prototype), s.prototype[k] = function() {
                return this;
            }, _.AsyncIterator = s, // Note that simple async functions are implemented on top of
            // AsyncIterator objects; they just return a Promise for the value of
            // the final result produced by the iterator.
            _.async = function(t, e, r, o) {
                var i = new s(n(t, e, r, o));
                return _.isGeneratorFunction(e) ? i : i.next().then(function(t) {
                    return t.done ? t.value : i.next();
                });
            }, // Define Generator.prototype.{next,throw,return} in terms of the
            // unified ._invoke helper method.
            u(F), F[x] = "Generator", // A Generator should always return itself as the iterator object when the
            // @@iterator function is called on it. Some browsers' implementations of the
            // iterator prototype chain incorrectly implement this, causing the Generator
            // object to not be returned from this call. This ensures that doesn't happen.
            // See https://github.com/facebook/regenerator/issues/274 for more details.
            F[b] = function() {
                return this;
            }, F.toString = function() {
                return "[object Generator]";
            }, _.keys = function(t) {
                var e = [];
                for (var n in t) e.push(n);
                // Rather than returning an object with a next method, we keep
                // things simple and return the next function itself.
                return e.reverse(), function n() {
                    for (;e.length; ) {
                        var r = e.pop();
                        if (r in t) return n.value = r, n.done = !1, n;
                    }
                    // To avoid creating an additional object, we just hang the .value
                    // and .done properties off the next function object itself. This
                    // also ensures that the minifier will not anonymize the function.
                    return n.done = !0, n;
                };
            }, _.values = y, d.prototype = {
                constructor: d,
                reset: function(t) {
                    if (this.prev = 0, this.next = 0, // Resetting context._sent for legacy support of Babel's
                    // function.sent implementation.
                    this.sent = this._sent = p, this.done = !1, this.delegate = null, this.method = "next", 
                    this.arg = p, this.tryEntries.forEach(h), !t) for (var e in this) // Not sure about the optimal order of these conditions:
                    "t" === e.charAt(0) && m.call(this, e) && !isNaN(+e.slice(1)) && (this[e] = p);
                },
                stop: function() {
                    this.done = !0;
                    var t = this.tryEntries[0], e = t.completion;
                    if ("throw" === e.type) throw e.arg;
                    return this.rval;
                },
                dispatchException: function(t) {
                    function e(e, r) {
                        // If the dispatched exception was caught by a catch block,
                        // then let that catch block handle the exception normally.
                        return i.type = "throw", i.arg = t, n.next = e, r && (n.method = "next", n.arg = p), 
                        !!r;
                    }
                    if (this.done) throw t;
                    for (var n = this, r = this.tryEntries.length - 1; r >= 0; --r) {
                        var o = this.tryEntries[r], i = o.completion;
                        if ("root" === o.tryLoc) // Exception thrown outside of any try block that could handle
                        // it, so set the completion value of the entire function to
                        // throw the exception.
                        return e("end");
                        if (o.tryLoc <= this.prev) {
                            var a = m.call(o, "catchLoc"), u = m.call(o, "finallyLoc");
                            if (a && u) {
                                if (this.prev < o.catchLoc) return e(o.catchLoc, !0);
                                if (this.prev < o.finallyLoc) return e(o.finallyLoc);
                            } else if (a) {
                                if (this.prev < o.catchLoc) return e(o.catchLoc, !0);
                            } else {
                                if (!u) throw new Error("try statement without catch or finally");
                                if (this.prev < o.finallyLoc) return e(o.finallyLoc);
                            }
                        }
                    }
                },
                abrupt: function(t, e) {
                    for (var n = this.tryEntries.length - 1; n >= 0; --n) {
                        var r = this.tryEntries[n];
                        if (r.tryLoc <= this.prev && m.call(r, "finallyLoc") && this.prev < r.finallyLoc) {
                            var o = r;
                            break;
                        }
                    }
                    o && ("break" === t || "continue" === t) && o.tryLoc <= e && e <= o.finallyLoc && (// Ignore the finally entry if control is not jumping to a
                    // location outside the try/catch block.
                    o = null);
                    var i = o ? o.completion : {};
                    return i.type = t, i.arg = e, o ? (this.method = "next", this.next = o.finallyLoc, 
                    S) : this.complete(i);
                },
                complete: function(t, e) {
                    if ("throw" === t.type) throw t.arg;
                    return "break" === t.type || "continue" === t.type ? this.next = t.arg : "return" === t.type ? (this.rval = this.arg = t.arg, 
                    this.method = "return", this.next = "end") : "normal" === t.type && e && (this.next = e), 
                    S;
                },
                finish: function(t) {
                    for (var e = this.tryEntries.length - 1; e >= 0; --e) {
                        var n = this.tryEntries[e];
                        if (n.finallyLoc === t) return this.complete(n.completion, n.afterLoc), h(n), S;
                    }
                },
                catch: function(t) {
                    for (var e = this.tryEntries.length - 1; e >= 0; --e) {
                        var n = this.tryEntries[e];
                        if (n.tryLoc === t) {
                            var r = n.completion;
                            if ("throw" === r.type) {
                                var o = r.arg;
                                h(n);
                            }
                            return o;
                        }
                    }
                    // The context.catch method must only be called with a location
                    // argument that corresponds to a known catch block.
                    throw new Error("illegal catch attempt");
                },
                delegateYield: function(t, e, n) {
                    // Deliberately forget the last sent value so that we don't
                    // accidentally pass it on to the delegate.
                    return this.delegate = {
                        iterator: y(t),
                        resultName: e,
                        nextLoc: n
                    }, "next" === this.method && (this.arg = p), S;
                }
            };
        }(// Among the various tricks for obtaining a reference to the global
        // object, this seems to be the most reliable technique that does not
        // use indirect eval (which violates Content Security Policy).
        "object" === (void 0 === H ? "undefined" : G(H)) ? H : "object" === ("undefined" == typeof window ? "undefined" : G(window)) ? window : "object" === ("undefined" == typeof self ? "undefined" : G(self)) ? self : H);
    }), Symbol.iterator), z = 9007199254740991, K = window, X = window, Y = X.String, J = window, V = J.Array, Q = window, W = Q.Object, Z = window, $ = Z.Date, tt = window, et = tt.parseInt, nt = window, rt = nt.ArrayBuffer, ot = window, it = ot.DataView, at = window, ut = at.FormData, st = window, ct = st.setTimeout, ft = window, lt = ft.clearTimeout, ht = window, dt = ht.decodeURIComponent, yt = window, vt = yt.TypeError, pt = window, wt = pt.Uint8Array, mt = window, gt = mt.Uint8ClampedArray, bt = window, kt = bt.Uint16Array, xt = window, Ot = xt.Uint32Array, _t = window, jt = _t.Int8Array, At = window, Et = At.Int16Array, Lt = window, St = Lt.Int32Array, Tt = window, Pt = Tt.Float32Array, Bt = window, Ft = Bt.Float64Array, Rt = window, Ut = Rt.XMLHttpRequest, It = window, Nt = It.Promise, Dt = window, qt = Dt.Blob, Ct = window, Gt = Ct.FileReader, Ht = 16, Mt = function() {
        function t() {
            n(this, t), this.seed = $.now(), this.registry = [];
        }
        return C(t, [ {
            key: "get",
            value: function() {
                var t = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : "", e = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : $.now(), n = "Symbol(" + t + ")" + (this.seed + this.registry.length).toString(Ht);
                return this.registry.push([ n, "" + t + e ]), n;
            }
        }, {
            key: "for",
            value: function(t) {
                if (f(t)) {
                    for (var e = this.registry.length, n = 0; n < e; n += 1) {
                        var r = this.registry[n];
                        if (t === r[1]) return r[0];
                    }
                    return this.get(t, "");
                }
                throw new vt("Symbol.for was called with non-string: " + t);
            }
        }, {
            key: "keyFor",
            value: function(t) {
                for (var e = this.registry.length, n = 0; n < e; n += 1) {
                    var r = this.registry[n];
                    if (t === r[0]) return r[1];
                }
            }
        }, {
            key: "Symbol",
            get: function() {
                function t(t, e) {
                    W.defineProperty(n, t, {
                        value: e
                    });
                }
                var e = this, n = function(t) {
                    return e.get(t);
                };
                return s([ "iterator", "match", "replace", "search", "split", "hasInstance", "isConcatSpreadable", "unscopables", "species", "toPrimitive", "toStringTag" ], function(e) {
                    t(e, n(e));
                }), t("for", function(t) {
                    return e.for(t);
                }), t("keyFor", function(t) {
                    return e.keyFor(t);
                }), n;
            }
        } ]), t;
    }(), zt = new Mt().Symbol;
    K.Symbol || (K.Symbol = zt);
    var Kt = V.prototype;
    Kt[M] || (Kt[M] = l);
    var Xt = V.prototype.push;
    V.from || (V.from = y), String.prototype.repeat || (String.prototype.repeat = p), 
    // import postMessage from '../postMessage';
    // import addEventListner from '../dom/addEventListener';
    K.immediateId || (K.immediateId = 0), K.immediateId += 1;
    var Yt = K.setImmediate, Jt = void 0;
    Jt = w, ct(function() {
        // if (postMessage) {
        // 	testImmediate(setImmediatePostMessage, function () {
        // 		if (setImmediateAvailable !== setImmediateNative) {
        // 			setImmediateAvailable = setImmediatePostMessage;
        // 		}
        // 	});
        // }
        Yt && m(Yt, function() {
            Jt = Yt;
        });
    });
    var Vt = function(t) {
        return Jt(t);
    }, Qt = 0, Wt = 1, Zt = 2, $t = 3, te = function t() {
        var e = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : null, r = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : null;
        n(this, t), /* eslint-disable no-use-before-define */
        this.promise = new ee(d), /* eslint-enable no-use-before-define */
        this.onResolved = e, this.onRejected = r;
    }, ee = function() {
        function t(e) {
            n(this, t), this.deferreds = [], this.state = Qt, this.exec(e);
        }
        return C(t, [ {
            key: "is",
            value: function(t) {
                return this.state === t;
            }
        }, {
            key: "exec",
            value: function(t) {
                var e = this, n = !1, r = function(t) {
                    n || (n = !0, e.resolve(t));
                }, o = function(t) {
                    n || (n = !0, e.reject(t));
                };
                try {
                    t(r, o);
                } catch (t) {
                    o(t);
                }
            }
        }, {
            key: "resolve",
            value: function(t) {
                try {
                    if (t === this) throw new vt("A promise cannot be resolved with itself");
                    this.value = t, g(t) ? (this.state = $t, this.exec(function(e, n) {
                        t.then(e, n);
                    })) : this.state = Wt, this.finish();
                } catch (t) {
                    this.reject(t);
                }
            }
        }, {
            key: "reject",
            value: function(t) {
                this.state = Zt, this.value = t, this.finish();
            }
        }, {
            key: "finish",
            value: function() {
                var t = this;
                s(this.deferreds, function(e) {
                    t.handle(e);
                }), this.deferreds = null;
            }
        }, {
            key: "handle",
            value: function(t) {
                /* eslint-enable consistent-this */
                for (/* eslint-disable consistent-this */
                var e = this; e.is($t); ) e = e.value;
                if (e.is(Qt)) return void h(e.deferreds, t);
                Vt(function() {
                    var n = t.promise, r = t.onResolved, o = void 0 === r ? null : r, i = t.onRejected, a = void 0 === i ? null : i, u = e.is(Wt), s = u ? o : a;
                    if (null === s) return void (u ? n.resolve(e.value) : n.reject(e.value));
                    var c = void 0;
                    try {
                        c = s(e.value);
                    } catch (t) {
                        return void n.reject(t);
                    }
                    n.resolve(c);
                });
            }
        }, {
            key: "catch",
            value: function(t) {
                return this.then(null, t);
            }
        }, {
            key: "then",
            value: function(t, e) {
                var n = new te(t, e);
                return this.handle(n), n.promise;
            }
        } ], [ {
            key: "resolve",
            value: function(e) {
                return g(e) ? e : new t(function(t) {
                    t(e);
                });
            }
        }, {
            key: "reject",
            value: function(e) {
                return new t(function(t, n) {
                    n(e);
                });
            }
        }, {
            key: "race",
            value: function(e) {
                return new t(function(t, n) {
                    s(e, function(e) {
                        e.then(t, n);
                    });
                });
            }
        }, {
            key: "all",
            value: function(e) {
                return new t(function(t, n) {
                    function r(o, a) {
                        if (g(o)) return void o.then(function(t) {
                            r(t, a);
                        }, n);
                        e[a] = o, 0 === (i -= 1) && t(e);
                    }
                    var o = e.length;
                    if (0 === o) return void t([]);
                    var i = o;
                    s(e, function(t, e) {
                        r(t, e);
                    });
                });
            }
        } ]), t;
    }();
    K.Promise = K.Promise || ee;
    var ne = function() {
        function t(e) {
            var r = this;
            n(this, t), this.clear(), e && s(e, function(t) {
                var e = q(t, 2), n = e[0], o = e[1];
                r.set(n, o);
            });
        }
        return C(t, [ {
            key: "clear",
            value: function() {
                this.data = [];
            }
        }, {
            key: "indexOfKey",
            value: function(t) {
                return k(this.data, function(e) {
                    return q(e, 1)[0] === t;
                });
            }
        }, {
            key: "has",
            value: function(t) {
                return 0 <= this.indexOfKey(t);
            }
        }, {
            key: "set",
            value: function(t, e) {
                var n = this.indexOfKey(t);
                return 0 <= n ? this.data[n][1] = e : h(this.data, [ t, e ]), this;
            }
        }, {
            key: "get",
            value: function(t) {
                var e = b(this.data, function(e) {
                    return q(e, 1)[0] === t;
                });
                if (e) return e[1];
            }
        }, {
            key: "delete",
            value: function(t) {
                var e = this.indexOfKey(t);
                return 0 <= e && (x(this.data, e, 1), !0);
            }
        }, {
            key: "entries",
            value: function() {
                return this.data[Symbol.iterator]();
            }
        }, {
            key: "forEach",
            value: function(t, e) {
                s(this.data, t, e);
            }
        }, {
            key: "keys",
            value: function() {
                var t = this.entries();
                return {
                    next: function() {
                        var e = t.next(), n = e.value, r = e.done;
                        return {
                            value: n && n[0],
                            done: r
                        };
                    }
                };
            }
        }, {
            key: "values",
            value: function() {
                var t = this.entries();
                return {
                    next: function() {
                        var e = t.next(), n = e.value, r = e.done;
                        return {
                            value: n && n[1],
                            done: r
                        };
                    }
                };
            }
        }, {
            key: "size",
            get: function() {
                return this.data.length;
            }
        } ]), t;
    }(), re = K.Map;
    re && 1 === new re([ [ 0, 0 ] ]).size && re.prototype.forEach || (re = ne), K.Map = re, 
    Map.prototype[M] || (Map.prototype[M] = O);
    var oe = function() {
        function t(e) {
            var r = this;
            n(this, t), this.clear(), e && s(e, function(t) {
                r.add(t);
            });
        }
        return C(t, [ {
            key: "clear",
            value: function() {
                this.data = [];
            }
        }, {
            key: "indexOf",
            value: function(t) {
                return this.data.indexOf(t);
            }
        }, {
            key: "has",
            value: function(t) {
                return 0 <= this.indexOf(t);
            }
        }, {
            key: "add",
            value: function(t) {
                return this.has(t) || h(this.data, t), this;
            }
        }, {
            key: "delete",
            value: function(t) {
                var e = this.indexOf(t);
                return 0 <= e && x(this.data, e, 1), 0 <= e;
            }
        }, {
            key: "forEach",
            value: function(t, e) {
                var n = this;
                s(this.data, function(r) {
                    t.call(e, r, r, n);
                });
            }
        }, {
            key: "values",
            value: function() {
                return this.data[M]();
            }
        }, {
            key: M,
            value: function() {
                return this.values();
            }
        }, {
            key: "entries",
            value: function() {
                var t = this.values();
                return {
                    next: function() {
                        var e = t.next(), n = e.value, r = e.done;
                        return {
                            value: !r && [ n, n ],
                            done: r
                        };
                    }
                };
            }
        }, {
            key: "size",
            get: function() {
                return this.data.length;
            }
        } ]), t;
    }(), ie = K.Set;
    ie && 1 === new ie([ 0 ]).size && ie.prototype.forEach || (ie = oe), K.Set = ie, 
    NodeList.prototype[M] || (NodeList.prototype[M] = _), HTMLCollection.prototype[M] || (HTMLCollection.prototype[M] = j), 
    NamedNodeMap.prototype[M] || (NamedNodeMap.prototype[M] = A);
    var ae = function() {
        function t(e) {
            var r = this;
            n(this, t), this.clear(), e && y(e, function(t) {
                var e = q(t, 2), n = e[0], o = e[1];
                r.append(n, o);
            });
        }
        return C(t, [ {
            key: "clear",
            value: function() {
                this.data = [];
            }
        }, {
            key: "indexOf",
            value: function(t) {
                return k(this.data, function(e) {
                    return q(e, 1)[0] === t;
                });
            }
        }, {
            key: "has",
            value: function(t) {
                return 0 <= this.indexOf(t);
            }
        }, {
            key: "append",
            value: function(t, e) {
                h(this.data, [ t, e ]);
            }
        }, {
            key: "set",
            value: function(t, e) {
                var n = this.indexOf(t);
                n < 0 ? this.append(t, e) : this.data[n][1] = e;
            }
        }, {
            key: "delete",
            value: function(t) {
                this.data = E(this.data, function(e) {
                    return q(e, 1)[0] !== t;
                });
            }
        }, {
            key: "get",
            value: function(t) {
                var e = b(this.data, function(e) {
                    return q(e, 1)[0] === t;
                });
                return e ? e[1] : null;
            }
        }, {
            key: "getAll",
            value: function(t) {
                var e = [];
                return s(this.data, function(n) {
                    var r = q(n, 2), o = r[0], i = r[1];
                    o === t && h(e, i);
                }), e;
            }
        }, {
            key: "toString",
            value: function() {
                return y(this.data, function(t) {
                    var e = q(t, 2), n = e[0], r = e[1];
                    return n + ":" + (void 0 === r ? "" : r);
                }).join(",");
            }
        }, {
            key: "entries",
            value: function() {
                return this.data[M]();
            }
        }, {
            key: "values",
            value: function() {
                var t = this.entries();
                return {
                    next: function() {
                        var e = t.next(), n = e.value, r = e.done;
                        return {
                            value: n && n[1],
                            done: r
                        };
                    }
                };
            }
        }, {
            key: M,
            value: function() {
                return this.entries();
            }
        } ]), t;
    }(), ue = "&", se = "=", ce = function(r) {
        function o(e) {
            return n(this, o), t(this, (o.__proto__ || Object.getPrototypeOf(o)).call(this, e ? y(e.replace(/^\?/, "").split(ue), function(t) {
                return t.split(se);
            }) : null));
        }
        return e(o, r), C(o, [ {
            key: "toString",
            value: function() {
                return y(this.data, function(t) {
                    var e = q(t, 2), n = e[0], r = e[1];
                    return n + "=" + (void 0 === r ? "" : r);
                }).join("&");
            }
        } ]), o;
    }(ae), fe = K.URLSearchParams;
    fe && new fe("?a=b").has("a") || (K.URLSearchParams = ce);
    var le = function(r) {
        function o(e) {
            n(this, o);
            var r = [];
            return e && L(e, function(t, e) {
                h(r, [ e, t ]);
            }), t(this, (o.__proto__ || Object.getPrototypeOf(o)).call(this, r));
        }
        return e(o, r), C(o, [ {
            key: "indexOf",
            value: function(t) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "indexOf", this).call(this, S(t));
            }
        }, {
            key: "has",
            value: function(t) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "has", this).call(this, S(t));
            }
        }, {
            key: "append",
            value: function(t, e) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "append", this).call(this, S(t), e);
            }
        }, {
            key: "set",
            value: function(t, e) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "set", this).call(this, S(t), e);
            }
        }, {
            key: "delete",
            value: function(t) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "delete", this).call(this, S(t));
            }
        }, {
            key: "get",
            value: function(t) {
                return D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "getAll", this).call(this, S(t)).join(",");
            }
        }, {
            key: "entries",
            value: function() {
                var t = this, e = D(o.prototype.__proto__ || Object.getPrototypeOf(o.prototype), "entries", this).call(this), n = [];
                return {
                    next: function() {
                        for (;;) {
                            var r = e.next(), o = r.value, i = r.done, a = o && o[0];
                            if (i || n.indexOf(a) < 0) return h(n, a), {
                                value: [ a, t.get(a) ],
                                done: i
                            };
                        }
                    }
                };
            }
        } ]), o;
    }(ae);
    K.Headers || (K.Headers = le);
    var he = [ wt, gt, kt, Ot, jt, Et, St, Pt, Ft ], de = JSON.parse, ye = 63, ve = [ ye, 127, 31, 15, 7, 3, 1 ], pe = 6, we = function() {
        function t() {
            n(this, t), this.bodyUsed = !1;
        }
        return C(t, [ {
            key: "initBody",
            value: function(t) {
                if (this.bodyInit = t, t) if (f(t)) this.bodyText = t; else if (P(t, qt)) this.bodyBlob = t; else if (P(t, ut)) this.bodyFormData = t; else if (P(t, URLSearchParams)) this.bodyText = t.toString(); else if (P(t, it)) this.bodyArrayBuffer = U(t.buffer), 
                // IE 10-11 can't handle a DataView body.
                this.bodyInit = new qt([ this.bodyArrayBuffer ]); else {
                    if (!P(t, rt) && !B(t)) throw new Error("unsupported BodyInit type");
                    this.bodyArrayBuffer = U(t);
                } else this.bodyText = "";
                this.headers.get("content-type") || (f(t) ? this.headers.set("content-type", "text/plain;charset=UTF-8") : this.bodyBlob && this.bodyBlob.type ? this.headers.set("content-type", this.bodyBlob.type) : t instanceof URLSearchParams && this.headers.set("content-type", "application/x-www-form-urlencoded;charset=UTF-8"));
            }
        }, {
            key: "arrayBuffer",
            value: function() {
                return this.bodyArrayBuffer ? this.isUsed || Nt.resolve(this.bodyArrayBuffer) : this.blob().then(function(t) {
                    return i(t, "ArrayBuffer");
                });
            }
        }, {
            key: "blob",
            value: function() {
                var t = this.isUsed;
                if (t) return t;
                if (this.bodyBlob) return Nt.resolve(this.bodyBlob);
                if (this.bodyArrayBuffer) return Nt.resolve(new qt([ this.bodyArrayBuffer ]));
                if (this.bodyFormData) throw new Error("could not read FormData body as blob");
                return Nt.resolve(new qt([ this.bodyText ]));
            }
        }, {
            key: "text",
            value: function() {
                var t = this.isUsed;
                if (t) return t;
                if (this.bodyBlob) return i(this.bodyBlob, "Text");
                if (this.bodyArrayBuffer) return Nt.resolve(R(this.bodyArrayBuffer));
                if (this.bodyFormData) throw new Error("could not read FormData body as text");
                return Nt.resolve(this.bodyText);
            }
        }, {
            key: "formData",
            value: function() {
                return this.text().then(c);
            }
        }, {
            key: "json",
            value: function() {
                return this.text().then(de);
            }
        }, {
            key: "isUsed",
            get: function() {
                if (this.bodyUsed) return Nt.reject(new vt("Already used"));
                this.bodyUsed = !0;
            }
        } ]), t;
    }(), me = function(r) {
        function o(e) {
            var r = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : {};
            n(this, o);
            var i = r.body, a = t(this, (o.__proto__ || Object.getPrototypeOf(o)).call(this));
            if (e instanceof o ? i = a.inheritFrom(e, i, r) : a.url = "" + e, a.credentials = r.credentials || a.credentials || "omit", 
            !r.headers && a.headers || (a.headers = new le(r.headers)), a.method = (r.method || a.method || "GET").toUpperCase(), 
            a.mode = r.mode || a.mode || null, a.referrer = null, ("GET" === a.method || "HEAD" === a.method) && i) throw new TypeError("Body not allowed for GET or HEAD requests");
            return a.initBody(i), a;
        }
        return e(o, r), C(o, [ {
            key: "inheritFrom",
            value: function(t, e, n) {
                var r = n.headers;
                if (t.bodyUsed) throw new TypeError("Already read");
                return this.url = t.url, this.credentials = t.credentials, r || (this.headers = new le(t.headers)), 
                this.method = t.method, this.mode = t.mode, e || null === t.bodyInit || (e = t.bodyInit, 
                t.bodyUsed = !0), e;
            }
        }, {
            key: "clone",
            value: function() {
                return new o(this, {
                    body: this.bodyInit
                });
            }
        } ]), o;
    }(we), ge = 200, be = 300, ke = [ 301, 302, 303, 307, 308 ], xe = function(r) {
        function o(e) {
            var r = arguments.length > 1 && void 0 !== arguments[1] ? arguments[1] : {};
            n(this, o);
            var i = t(this, (o.__proto__ || Object.getPrototypeOf(o)).call(this));
            return i.type = "default", i.status = "status" in r ? r.status : ge, i.ok = i.status >= ge && i.status < be, 
            i.statusText = "statusText" in r ? r.statusText : "OK", i.headers = new le(r.headers), 
            i.url = r.url || "", i.initBody(e), i;
        }
        return e(o, r), C(o, [ {
            key: "clone",
            value: function() {
                return new o(this.bodyInit, {
                    status: this.status,
                    statusText: this.statusText,
                    headers: new le(this.headers),
                    url: this.url
                });
            }
        }, {
            key: "redirect",
            value: function(t, e) {
                if (ke.indexOf(e) < 0) throw new RangeError("Invalid status code");
                return new o(null, {
                    status: e,
                    headers: {
                        location: t
                    }
                });
            }
        } ], [ {
            key: "error",
            value: function() {
                var t = new o(null, {
                    status: 0,
                    statusText: ""
                });
                return t.type = "error", t;
            }
        } ]), o;
    }(we);
    // if (!window.fetch) {
    // 	window.fetch = j0Fetch;
    // }
    K.fetch = N, K.Body || (K.Body = we), K.Response || (K.Response = xe), K.Request || (K.Request = me), 
    K.requestAnimationFrame = K.requestAnimationFrame || K.mozRequestAnimationFrame || K.webkitRequestAnimationFrame || K.msRequestAnimationFrame || function(t) {
        return ct(function() {
            t($.now());
        }, 30);
    }, K.cancelAnimationFrame = K.cancelAnimationFrame || K.mozCancelAnimationFrame || function(t) {
        return lt(t);
    }, K.global = K;
}();