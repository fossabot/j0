describe('querySelectorAll', function () {

	var assert = require('assert');
	var querySelectorAll = require('../lib/querySelectorAll');

	it('should do X', function () {
		assert.equal(0, 1);
	});

});