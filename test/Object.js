describe('Object', function () {

	var assert = require('assert');
	var Object = require('../lib/Object');

	it('should be the constructor of an array', function () {
		assert.equal(assert instanceof Object, true);
	});

});
