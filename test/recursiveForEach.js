describe('recursiveForEach', function () {

	var assert = require('assert');
	var recursiveForEach = require('../lib/recursiveForEach');

	it('should do X', function () {
		assert.equal(0, 1);
	});

});